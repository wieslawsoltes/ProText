# ProText.MAUI.Benchmarks

BenchmarkDotNet coverage for the `ProText.MAUI` implementation.

The project includes MAUI `Label` and `Editor` baseline measurement benchmarks. When `src/ProText.MAUI` exists, it also activates ProTextBlock, ProTextPresenter, and ProTextBox benchmark surfaces through the local project reference and reflection-based control creation.
