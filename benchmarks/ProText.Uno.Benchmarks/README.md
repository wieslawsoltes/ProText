# ProText.Uno.Benchmarks

BenchmarkDotNet coverage for the `ProText.Uno` implementation.

The project includes WinUI baseline measurement benchmarks and ProTextBlock, ProTextPresenter, and ProTextBox benchmark surfaces by default through the local `src/ProText.Uno` project reference.
